//The unshift() method adds an element at the beginning of the array. For example,

let student = ['class:b.tech' , 'age:22']

//add element at the begin

student.unshift('name:mo.riyaj' , 'branch:computer science');

console.log(student);